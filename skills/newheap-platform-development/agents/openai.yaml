interface:
  display_name: "NewHeap Platform Development"
  short_description: "Build consumers with deliberate NewHeap fit"
  default_prompt: "Use $newheap-platform-development to route the smallest relevant module, verify current Platform capabilities when applicable, and deliberately choose reuse, consumer ownership, or a separate upstream suggestion."
